<%= form_with(model: reach) do |form| %>
  <% if reach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reach.errors.count, "error") %> prohibited this reach from being saved:</h2>

      <ul>
        <% reach.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
