<%= form_with(model: ruling) do |form| %>
  <% if ruling.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ruling.errors.count, "error") %> prohibited this ruling from being saved:</h2>

      <ul>
        <% ruling.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
